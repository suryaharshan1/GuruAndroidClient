<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.windroilla.guru.ProfilePictureDisplay">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dip"
        android:layout_weight="1"
        android:gravity="center"
        android:orientation="vertical">

        <ImageView
            android:id="@+id/display_picture"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_margin="0dip"
            android:contentDescription="@string/profile_picture"
            android:padding="0dip" />

    </LinearLayout>

    <LinearLayout
        android:id="@+id/display_bottom_panel"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="@color/guru_blue"
        android:gravity="center"
        android:orientation="horizontal"
        android:paddingBottom="8dp"
        android:paddingTop="8dp">

        <ImageButton
            android:id="@+id/display_select_camera"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_marginRight="16dip"
            android:background="@null"
            android:contentDescription="@string/display_camera_button"
            android:onClick="OnImageModeSelection"
            android:padding="6dip"
            android:src="@android:drawable/ic_menu_camera" />

        <ImageButton
            android:id="@+id/display_select_gallery"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:background="@null"
            android:contentDescription="@string/display_camera_button"
            android:onClick="OnImageModeSelection"
            android:padding="6dip"
            android:src="@android:drawable/ic_menu_gallery" />

    </LinearLayout>
</LinearLayout>
